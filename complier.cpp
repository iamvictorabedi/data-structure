#include <iostream>
using namespace std;

int main(){
	string input;
	cout<< "Enter  a string containing delimters and no whitespace"<<endl;
	cin>> input;
	if(inout.length() == 1)
	return 0;
	
	StackX the Stack;
	
	for(int j =0; j< input.length(); j++){
		char ch = input[j];
		
		switch(ch){
			case'<':
				case '[':
					case '(':
						if(!theStack.isEmpty()){
							char chx = thisStack.pop();
							if(ch=='>'&& chx!='<') (ch==']' && chx!='['))
						}
						
		}
	}
}
